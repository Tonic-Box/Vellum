package com.tonic.vellum.layout;

/** The axis a {@link Split} divides along. Constraints apply to the main axis; the cross axis gets the full extent. */
public enum Axis {
    HORIZONTAL,
    VERTICAL
}
